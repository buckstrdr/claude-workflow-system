#!/usr/bin/env bash
# After branch checkout, do a fast session refresh (semantic only).
set -euo pipefail
ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

if command -v make >/dev/null 2>&1; then
  make -s session-refresh || true
fi
